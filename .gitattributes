# Normalize line endings to LF in the repo (consistent across Windows/POSIX).
* text=auto eol=lf

# Treat common binaries as binary (no EOL conversion).
*.db binary
*.sqlite binary
*.sqlite3 binary
*.png binary
*.jpg binary
*.ico binary
