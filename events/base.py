"""Events interface.

Stage 2. ``Event(symbol, date, kind {EARNINGS, DIV}, confirmed, meta)`` plus
``get_events(symbol, window)``, ``get_next_earnings(symbol)``,
``get_next_exdiv(symbol)``. (The Event model itself is defined in core.models
as a placeholder in Stage 0.)
"""
