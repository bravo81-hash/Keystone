# Keystone runtime + test dependencies (no live TWS connection needed).
# Live connectivity dependency (ib_insync) lives in requirements-live.txt so CI
# and tests stay light and never require a TWS gateway.
flask>=3.0
pydantic>=2.5
pyyaml>=6.0
pytest>=8.0
# IANA time-zone database for zoneinfo. Required on Windows (no system tzdata);
# harmless elsewhere. Keystone is anchored to America/New_York throughout.
tzdata>=2024.1
